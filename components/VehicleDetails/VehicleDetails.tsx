import { CamperCar } from '@/types/types';
import styles from './VehicleDetails.module.css';

interface DetailsProps {
    data: CamperCar;
}

export default function VehicleDetails({data}: DetailsProps){
const details = data;
return(
    <div></div>
)
}