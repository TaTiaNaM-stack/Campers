import styles from './CamperMainInfo.module.css';
import {CamperCar} from '@/types/types';

interface InfoProps {
    data: CamperCar;
}

export default function CamperMainInfo({data}: InfoProps){
const info = data;
return (
    <div></div>
)
}